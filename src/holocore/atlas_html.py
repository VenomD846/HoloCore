"""Dependency-free, self-contained HTML rendering for a HoloCore Atlas graph."""
from __future__ import annotations

import json
from collections.abc import Iterable, Mapping
from pathlib import Path
from typing import Any


ATLAS_VIEWER_VERSION = "3"


def _validated_graph(graph: Mapping[str, Any]) -> dict[str, Any]:
    nodes = graph.get("nodes", [])
    links = graph.get("links", graph.get("edges", []))
    if not isinstance(nodes, list) or not isinstance(links, list):
        raise ValueError("Atlas graph must contain node and link arrays")
    return {**graph, "nodes": nodes, "links": links}


def _script_json(value: Any) -> str:
    return (
        json.dumps(value, ensure_ascii=False, separators=(",", ":"))
        .replace("&", "\\u0026")
        .replace("<", "\\u003c")
        .replace(">", "\\u003e")
        .replace("\u2028", "\\u2028")
        .replace("\u2029", "\\u2029")
    )


def render_atlas_html(graph: Mapping[str, Any], *, title: str = "HoloCore Atlas") -> str:
    """Return an accessible standalone force-directed Atlas viewer."""
    if graph.get("graph", {}).get("generator") == "holocore-global-atlas":
        return _render_solar_atlas(graph, title)
    payload = _script_json(_validated_graph(graph))
    safe_title = (
        title.replace("&", "&amp;").replace("<", "&lt;")
        .replace(">", "&gt;").replace('"', "&quot;")
    )
    template = r'''<!doctype html>
<html lang="en"><head><meta charset="utf-8"><meta name="viewport" content="width=device-width,initial-scale=1"><meta name="holocore-atlas-viewer" content="3">
<title>__TITLE__</title><style>
:root{color-scheme:dark;font-family:"IBM Plex Sans","Segoe UI",system-ui,sans-serif;background:#0f172a;color:#f8fafc;--panel:#111c30;--line:#334155;--muted:#a8b4c5;--focus:#38bdf8;--accent:#22c55e}*{box-sizing:border-box}body{margin:0;height:100vh;overflow:hidden;display:grid;grid-template-rows:auto 1fr;background:#0f172a}.bar{z-index:2;min-height:64px;padding:10px 14px;background:#111c30;border-bottom:1px solid #334155;display:flex;gap:8px;align-items:center;flex-wrap:wrap;box-shadow:0 4px 18px #02061766}.brand{display:flex;align-items:center;gap:10px;margin-right:auto}.brand-mark{width:30px;height:30px;border-radius:9px;display:grid;place-items:center;background:#0ea5e9;color:#03121f;font:700 17px "JetBrains Mono",monospace}.brand strong{font-size:16px;letter-spacing:.01em}.control{height:40px;background:#0b1324;color:#e2e8f0;border:1px solid #475569;border-radius:8px;padding:0 11px;font:inherit}.control:hover{border-color:#64748b}.control:focus-visible,button:focus-visible{outline:3px solid #38bdf8;outline-offset:2px}input.control{width:min(290px,42vw)}button.control{cursor:pointer;min-width:40px;font-weight:600}.icon-button{font-size:18px;padding:0 12px}.layout{min-height:0;display:grid;grid-template-columns:minmax(0,1fr) 320px}#graph{position:relative;min-width:0;min-height:0;background:radial-gradient(circle at 48% 45%,#172554 0,#111c30 30%,#0b1324 68%,#07101e 100%);overflow:hidden;touch-action:none}.canvas-help{position:absolute;left:14px;bottom:12px;z-index:1;padding:7px 10px;border:1px solid #334155;border-radius:8px;background:#0f172add;color:#b8c4d4;font-size:12px;pointer-events:none}.loading{position:absolute;inset:0;display:grid;place-items:center;color:#cbd5e1;font-size:14px;background:#0b1324}.loading[hidden]{display:none}aside{overflow:auto;padding:18px;border-left:1px solid #334155;background:#111c30}aside h2{font-size:16px;margin:0 0 14px}.muted{color:#a8b4c5;font-size:13px;line-height:1.45}.field{margin:0 0 13px;overflow-wrap:anywhere}.field b{display:block;margin-bottom:3px;color:#e2e8f0;font-size:12px;text-transform:uppercase;letter-spacing:.05em}.legend{display:flex;flex-wrap:wrap;gap:6px;margin-top:16px}.chip{display:inline-flex;align-items:center;gap:6px;padding:4px 7px;border:1px solid #334155;border-radius:999px;color:#cbd5e1;font-size:11px}.dot{width:8px;height:8px;border-radius:50%}svg{display:block;width:100%;height:100%;cursor:grab}svg.panning{cursor:grabbing}.edge{stroke:#64748b;stroke-opacity:.28;stroke-width:1}.edge.active{stroke:#7dd3fc;stroke-opacity:.8;stroke-width:1.7}.node{cursor:pointer;stroke:#0f172a;stroke-width:2;transition:stroke-width .15s,filter .15s}.node:hover,.node.selected{stroke:#f8fafc;stroke-width:3;filter:drop-shadow(0 0 5px #38bdf8)}.label{fill:#cbd5e1;font-size:11px;font-weight:500;paint-order:stroke;stroke:#0b1324;stroke-width:4px;stroke-linejoin:round;pointer-events:none;opacity:0}.label.major,.label.match,.node-group:hover .label,.node-group.selected .label{opacity:1}.label.suppressed{opacity:0}.node-group:hover .label.suppressed,.node-group.selected .label.suppressed{opacity:1}.empty{position:absolute;inset:0;display:grid;place-items:center;color:#b8c4d4}.toggle{display:flex;align-items:center;gap:6px;color:#cbd5e1;font-size:12px;padding:0 4px}.toggle input{accent-color:#22c55e}.stats{min-width:160px;text-align:right}.sr-only{position:absolute;width:1px;height:1px;padding:0;margin:-1px;overflow:hidden;clip:rect(0,0,0,0);white-space:nowrap;border:0}
@media(max-width:900px){body{overflow:auto;height:auto;min-height:100vh}.layout{grid-template-columns:1fr;grid-template-rows:70vh auto}.stats{width:100%;text-align:left}.bar{position:sticky;top:0}.layout aside{border-left:0;border-top:1px solid #334155;min-height:260px}input.control{width:min(100%,320px)}}
@media(prefers-reduced-motion:reduce){*{scroll-behavior:auto!important;transition:none!important}}
</style></head><body>
<header class="bar"><div class="brand"><span class="brand-mark" aria-hidden="true">H</span><strong>__TITLE__</strong></div><label class="sr-only" for="search">Search Signals</label><input id="search" class="control" type="search" placeholder="Search concepts, decisions, or sources" autocomplete="off"><select id="mode" class="control" aria-label="Atlas view"><option value="knowledge">Knowledge</option><option value="structure">Code structure</option><option value="all">Everything</option></select><select id="kind" class="control" aria-label="Filter Signal type"><option value="">All Signal types</option></select><select id="relation" class="control" aria-label="Filter relationship"><option value="">All relationships</option></select><button id="zoomOut" class="control icon-button" type="button" aria-label="Zoom out">−</button><button id="fit" class="control" type="button">Fit</button><button id="zoomIn" class="control icon-button" type="button" aria-label="Zoom in">+</button><button id="reset" class="control" type="button">Reset</button><label class="toggle"><input id="labels" type="checkbox"> More labels</label><span id="stats" class="muted stats" aria-live="polite"></span></header>
<main class="layout"><div id="graph"><div id="loading" class="loading">Arranging the Atlas…</div><div class="canvas-help">Drag nodes · drag background to pan · scroll to zoom</div></div><aside><h2>Signal details</h2><div id="details" class="muted">Select a Signal to inspect its source, type, and relationships.</div><div id="legend" class="legend" aria-label="Signal type legend"></div></aside></main>
<script id="atlas-data" type="application/json">__ATLAS_DATA__</script>
<script>
(()=>{'use strict';
const data=JSON.parse(document.getElementById('atlas-data').textContent),allNodes=data.nodes||[],allLinks=data.links||data.edges||[];
const byId=new Map(allNodes.map(n=>[String(n.id),n])),search=document.getElementById('search'),mode=document.getElementById('mode'),kind=document.getElementById('kind'),relation=document.getElementById('relation'),host=document.getElementById('graph'),details=document.getElementById('details'),stats=document.getElementById('stats'),loading=document.getElementById('loading'),moreLabels=document.getElementById('labels');
const esc=v=>String(v??'').replace(/[&<>"']/g,c=>({'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot;',"'":'&#39;'}[c]));
const endpoint=v=>String(v&&typeof v==='object'?v.id:v),nodeKind=n=>String(n.kind||n.type||n.file_type||'unknown');
const isKnowledge=n=>Boolean(n.semantic)||String(n.source_file||'').startsWith('.knowledge/');
if(!allNodes.some(isKnowledge))mode.value='all';
const palette=['#38bdf8','#22c55e','#f59e0b','#a78bfa','#f472b6','#2dd4bf','#fb7185','#94a3b8'];
const hash=s=>{let h=2166136261;for(const c of String(s)){h^=c.charCodeAt(0);h=Math.imul(h,16777619)}return h>>>0};
const color=s=>palette[hash(s)%palette.length];
function options(el,values){[...new Set(values.filter(Boolean))].sort().forEach(v=>{const o=document.createElement('option');o.value=v;o.textContent=v;el.appendChild(o)})}
options(kind,allNodes.map(nodeKind));options(relation,allLinks.map(e=>String(e.relation||e.type||'')));
document.getElementById('legend').innerHTML=[...new Set(allNodes.map(nodeKind))].sort().slice(0,16).map(k=>`<span class="chip"><span class="dot" style="background:${color(k)}"></span>${esc(k)}</span>`).join('');
function show(n,degree){const fields=[['Label',n.label||n.id],['Type',nodeKind(n)],['Connections',degree],...Object.entries(n).filter(([k,v])=>!['label','kind','type'].includes(k)&&v!==null&&v!==''&&typeof v!=='object')];details.innerHTML=fields.map(([k,v])=>`<div class="field"><b>${esc(k)}</b>${esc(v)}</div>`).join('')||'<span class="muted">No details.</span>'}
let svg,viewport,transform={x:0,y:0,k:1},positions=new Map(),selected=null,visibleNodes=[],visibleLinks=[],degrees=new Map();
function forceLayout(nodes,links,w,h){const pos=new Map(),vel=new Map(),degree=new Map(nodes.map(n=>[String(n.id),0]));links.forEach(e=>{const a=endpoint(e.source??e.from),b=endpoint(e.target??e.to);degree.set(a,(degree.get(a)||0)+1);degree.set(b,(degree.get(b)||0)+1)});const kinds=[...new Set(nodes.map(nodeKind))].sort(),kindIndex=new Map(kinds.map((k,i)=>[k,i]));nodes.forEach(n=>{const seed=hash(n.id),angle=(seed%6283)/1000,spread=70+((seed>>>8)%1000)/1000*Math.min(w,h)*.32,cluster=(kindIndex.get(nodeKind(n))||0)/Math.max(1,kinds.length)*Math.PI*2;pos.set(String(n.id),{x:w/2+Math.cos(angle)*spread+Math.cos(cluster)*w*.16,y:h/2+Math.sin(angle)*spread+Math.sin(cluster)*h*.16});vel.set(String(n.id),{x:0,y:0})});const iterations=nodes.length>1200?45:nodes.length>500?70:120,cell=44;for(let step=0;step<iterations;step++){const grid=new Map();nodes.forEach(n=>{const p=pos.get(String(n.id)),key=`${Math.floor(p.x/cell)},${Math.floor(p.y/cell)}`;(grid.get(key)||grid.set(key,[]).get(key)).push(n)});for(const n of nodes){const id=String(n.id),p=pos.get(id),v=vel.get(id),ci=Math.floor(p.x/cell),cj=Math.floor(p.y/cell);v.x+=(w/2-p.x)*.0008;v.y+=(h/2-p.y)*.0008;for(let i=ci-1;i<=ci+1;i++)for(let j=cj-1;j<=cj+1;j++)for(const other of grid.get(`${i},${j}`)||[]){const oid=String(other.id);if(oid===id||oid<id)continue;const q=pos.get(oid),ov=vel.get(oid),dx=p.x-q.x,dy=p.y-q.y,d=Math.max(1,Math.hypot(dx,dy)),min=18+Math.min(12,(degree.get(id)||0)+(degree.get(oid)||0));if(d<min){const f=(min-d)*.018,fx=dx/d*f,fy=dy/d*f;v.x+=fx;v.y+=fy;ov.x-=fx;ov.y-=fy}}}for(const e of links){const a=endpoint(e.source??e.from),b=endpoint(e.target??e.to),p=pos.get(a),q=pos.get(b);if(!p||!q)continue;const av=vel.get(a),bv=vel.get(b),dx=q.x-p.x,dy=q.y-p.y,d=Math.max(1,Math.hypot(dx,dy)),target=58+Math.min(55,Math.log2(2+(degree.get(a)||0)+(degree.get(b)||0))*9),f=(d-target)*.0028,fx=dx/d*f,fy=dy/d*f;av.x+=fx;av.y+=fy;bv.x-=fx;bv.y-=fy}for(const n of nodes){const id=String(n.id),p=pos.get(id),v=vel.get(id);v.x*=.78;v.y*=.78;p.x=Math.max(18,Math.min(w-18,p.x+v.x));p.y=Math.max(18,Math.min(h-18,p.y+v.y))}}return{pos,degree}}
function applyTransform(){if(viewport)viewport.setAttribute('transform',`translate(${transform.x} ${transform.y}) scale(${transform.k})`)}
function fit(){transform={x:0,y:0,k:1};applyTransform()}
function zoom(factor,cx=host.clientWidth/2,cy=host.clientHeight/2){const next=Math.max(.18,Math.min(6,transform.k*factor));transform.x=cx-(cx-transform.x)*(next/transform.k);transform.y=cy-(cy-transform.y)*(next/transform.k);transform.k=next;applyTransform()}
function deconflictLabels(layer){const placed=[];layer.querySelectorAll('.label.major,.label.match').forEach(label=>{label.classList.remove('suppressed');const b=label.getBBox(),box={x:b.x-3,y:b.y-2,w:b.width+6,h:b.height+4};if(placed.some(p=>box.x<p.x+p.w&&box.x+box.w>p.x&&box.y<p.y+p.h&&box.y+box.h>p.y))label.classList.add('suppressed');else placed.push(box)})}
function draw(){loading.hidden=false;requestAnimationFrame(()=>{const q=search.value.trim().toLowerCase(),m=mode.value,k=kind.value,r=relation.value;let nodes=allNodes.filter(n=>(m==='all'||(m==='knowledge'&&isKnowledge(n))||(m==='structure'&&!isKnowledge(n)))&&(!k||nodeKind(n)===k)&&(!q||[n.id,n.label,n.source_file,n.module,n.qualified_name].some(v=>String(v||'').toLowerCase().includes(q))));let ids=new Set(nodes.map(n=>String(n.id)));let links=allLinks.filter(e=>ids.has(endpoint(e.source??e.from))&&ids.has(endpoint(e.target??e.to))&&(!r||String(e.relation||e.type||'')===r));if(r){const connected=new Set(links.flatMap(e=>[endpoint(e.source??e.from),endpoint(e.target??e.to)]));nodes=nodes.filter(n=>connected.has(String(n.id)));ids=connected}visibleNodes=nodes;visibleLinks=links;stats.textContent=`${nodes.length} ${m} Signals · ${links.length} relationships`;host.querySelectorAll('svg,.empty').forEach(el=>el.remove());if(!nodes.length){const e=document.createElement('div');e.className='empty';e.textContent='No matching Signals';host.appendChild(e);loading.hidden=true;return}const w=Math.max(host.clientWidth,500),h=Math.max(host.clientHeight,380),laid=forceLayout(nodes,links,w,h);positions=laid.pos;degrees=laid.degree;const ns='http://www.w3.org/2000/svg';svg=document.createElementNS(ns,'svg');svg.setAttribute('viewBox',`0 0 ${w} ${h}`);svg.setAttribute('role','img');svg.setAttribute('aria-label',`Atlas graph with ${nodes.length} Signals and ${links.length} relationships`);viewport=document.createElementNS(ns,'g');const edgeLayer=document.createElementNS(ns,'g'),nodeLayer=document.createElementNS(ns,'g');viewport.append(edgeLayer,nodeLayer);svg.appendChild(viewport);const connected=new Map();links.forEach(e=>{const a=endpoint(e.source??e.from),b=endpoint(e.target??e.to),p=positions.get(a),q=positions.get(b);if(!p||!q)return;(connected.get(a)||connected.set(a,[]).get(a)).push(b);(connected.get(b)||connected.set(b,[]).get(b)).push(a);const line=document.createElementNS(ns,'line');line.setAttribute('class','edge');line.dataset.a=a;line.dataset.b=b;line.setAttribute('x1',p.x);line.setAttribute('y1',p.y);line.setAttribute('x2',q.x);line.setAttribute('y2',q.y);edgeLayer.appendChild(line)});const major=new Set([...nodes].sort((a,b)=>(degrees.get(String(b.id))||0)-(degrees.get(String(a.id))||0)).slice(0,moreLabels.checked?120:35).map(n=>String(n.id)));nodes.forEach(n=>{const id=String(n.id),p=positions.get(id),g=document.createElementNS(ns,'g'),c=document.createElementNS(ns,'circle'),t=document.createElementNS(ns,'text'),d=degrees.get(id)||0,radius=Math.min(11,4.5+Math.sqrt(d)*1.25);g.setAttribute('class','node-group');g.dataset.id=id;c.setAttribute('class','node');c.setAttribute('cx',p.x);c.setAttribute('cy',p.y);c.setAttribute('r',radius);c.setAttribute('fill',color(nodeKind(n)));c.setAttribute('tabindex','0');c.setAttribute('role','button');c.setAttribute('aria-label',`${n.label||n.id}, ${nodeKind(n)}, ${d} connections`);t.setAttribute('class',`label${major.has(id)?' major':''}${q?' match':''}`);t.setAttribute('x',p.x+radius+5);t.setAttribute('y',p.y+4);t.textContent=String(n.label||n.id);const select=()=>{selected=id;nodeLayer.querySelectorAll('.node-group').forEach(x=>x.classList.toggle('selected',x.dataset.id===id));edgeLayer.querySelectorAll('.edge').forEach(x=>x.classList.toggle('active',x.dataset.a===id||x.dataset.b===id));show(n,d)};c.addEventListener('click',select);c.addEventListener('keydown',e=>{if(e.key==='Enter'||e.key===' '){e.preventDefault();select()}});let dragging=false;c.addEventListener('pointerdown',e=>{e.stopPropagation();dragging=true;c.setPointerCapture(e.pointerId)});c.addEventListener('pointermove',e=>{if(!dragging)return;const rect=svg.getBoundingClientRect(),x=(e.clientX-rect.left-transform.x)/transform.k,y=(e.clientY-rect.top-transform.y)/transform.k;p.x=x;p.y=y;c.setAttribute('cx',x);c.setAttribute('cy',y);t.setAttribute('x',x+radius+5);t.setAttribute('y',y+4);edgeLayer.querySelectorAll('.edge').forEach(line=>{if(line.dataset.a===id){line.setAttribute('x1',x);line.setAttribute('y1',y)}if(line.dataset.b===id){line.setAttribute('x2',x);line.setAttribute('y2',y)}})});c.addEventListener('pointerup',()=>dragging=false);g.append(c,t);nodeLayer.appendChild(g)});let pan=null;svg.addEventListener('pointerdown',e=>{if(e.target!==svg)return;pan={x:e.clientX,y:e.clientY,tx:transform.x,ty:transform.y};svg.classList.add('panning');svg.setPointerCapture(e.pointerId)});svg.addEventListener('pointermove',e=>{if(!pan)return;transform.x=pan.tx+e.clientX-pan.x;transform.y=pan.ty+e.clientY-pan.y;applyTransform()});svg.addEventListener('pointerup',()=>{pan=null;svg.classList.remove('panning')});svg.addEventListener('wheel',e=>{e.preventDefault();const rect=svg.getBoundingClientRect();zoom(e.deltaY<0?1.16:.86,e.clientX-rect.left,e.clientY-rect.top)},{passive:false});host.appendChild(svg);deconflictLabels(nodeLayer);fit();loading.hidden=true})}
let timer;search.addEventListener('input',()=>{clearTimeout(timer);timer=setTimeout(draw,120)});[mode,kind,relation,moreLabels].forEach(el=>el.addEventListener('change',draw));document.getElementById('zoomIn').addEventListener('click',()=>zoom(1.25));document.getElementById('zoomOut').addEventListener('click',()=>zoom(.8));document.getElementById('fit').addEventListener('click',fit);document.getElementById('reset').addEventListener('click',()=>{search.value=kind.value=relation.value='';mode.value=allNodes.some(isKnowledge)?'knowledge':'all';moreLabels.checked=false;selected=null;details.textContent='Select a Signal to inspect its source, type, and relationships.';draw()});let resizeTimer;addEventListener('resize',()=>{clearTimeout(resizeTimer);resizeTimer=setTimeout(draw,180)});draw();
})();
</script></body></html>'''
    return template.replace("__TITLE__", safe_title).replace("__ATLAS_DATA__", payload)


def _render_solar_atlas(graph: Mapping[str, Any], title: str) -> str:
    """Render a dense, Obsidian-inspired 3D graph across registered Worlds."""
    data = _script_json(_validated_graph(graph))
    safe = title.replace("&", "&amp;").replace("<", "&lt;").replace(">", "&gt;")
    template = r'''<!doctype html><html lang="en"><head><meta charset="utf-8"><meta name="viewport" content="width=device-width,initial-scale=1"><meta name="holocore-atlas-viewer" content="5"><title>__TITLE__</title><style>:root{color-scheme:dark;font:13px "IBM Plex Sans","Segoe UI",system-ui,sans-serif}*{box-sizing:border-box}body{margin:0;background:#000;color:#d9e2f2;overflow:hidden}header{height:58px;padding:10px 16px;display:flex;align-items:center;gap:14px;background:#090b11;border-bottom:1px solid #242b39;position:relative;z-index:2}h1{margin:0;color:#b6c8e8;font-size:15px}#stats{margin-left:auto;color:#8794aa}#hint{color:#667286;font-size:12px}#stage{position:relative;height:calc(100vh - 58px);background:radial-gradient(circle at 55% 50%,#121622 0,#06080d 48%,#000 90%)}canvas{width:100%;height:100%;display:block;cursor:grab}canvas.drag{cursor:grabbing}#panel{position:absolute;left:14px;top:14px;width:232px;max-height:calc(100% - 28px);overflow:auto;padding:10px;border:1px solid #252d3b;border-radius:8px;background:#0b0e16e8;box-shadow:0 12px 30px #000a;backdrop-filter:blur(10px)}#panel h2{font-size:11px;color:#7c8aa3;letter-spacing:.12em;text-transform:uppercase;margin:9px 2px}.world{display:block;width:100%;margin:5px 0;padding:8px 9px;background:#111722;border:1px solid #283346;border-radius:6px;color:#d6e2f4;text-align:left;cursor:pointer}.world:hover,.world.active{border-color:#7aa5ff;background:#182236}.world small{display:block;color:#75839a;margin-top:2px}.legend{display:flex;flex-wrap:wrap;gap:5px;color:#8a97aa;font-size:11px}.legend span{padding:3px 5px;border:1px solid #263044;border-radius:4px}</style></head><body><header><h1>__TITLE__</h1><span id="stats"></span><span id="hint">click a World or Signal · drag to orbit · wheel to zoom</span></header><div id="stage"><aside id="panel"><div id="worldList"></div><h2>Graph view</h2><div class="legend"><span>● World</span><span>● Signal</span><span>━ link</span></div></aside><canvas id="gl" aria-label="3D linked HoloCore Atlas"></canvas></div><script id="data" type="application/json">__DATA__</script><script>
(()=>{'use strict';const D=JSON.parse(document.getElementById('data').textContent),N=D.nodes||[],L=D.links||D.edges||[],W=N.filter(n=>String(n.kind||'').toLowerCase()==='world'),canvas=document.getElementById('gl'),stats=document.getElementById('stats'),list=document.getElementById('worldList');stats.textContent=`${W.length} Worlds · ${N.length} Signals · ${L.length} links`;const gl=canvas.getContext('webgl2',{antialias:true,alpha:false})||canvas.getContext('webgl',{antialias:true,alpha:false});if(!gl){stats.textContent+=' · WebGL unavailable';return}gl.enable(gl.DEPTH_TEST);gl.enable(gl.BLEND);gl.blendFunc(gl.SRC_ALPHA,gl.ONE_MINUS_SRC_ALPHA);const hash=s=>{let h=2166136261;for(const c of String(s)){h^=c.charCodeAt(0);h=Math.imul(h,16777619)}return h>>>0},wid=n=>String(n.world||n.id.split(':')[0]),wids=new Set(W.map(w=>wid(w))),worldNodes=new Map(W.map(w=>[wid(w),w])),children=new Map(W.map(w=>[wid(w),[]]));N.forEach(n=>{if(!worldNodes.has(wid(n)))children.set(wid(n),[]);else if(String(n.kind||'').toLowerCase()!=='world')children.get(wid(n)).push(n)});const centers=new Map();W.forEach((w,i)=>{const a=i/Math.max(1,W.length)*Math.PI*2,r=1.15+Math.sqrt(i+1)*.28;centers.set(wid(w),{x:Math.cos(a)*r,y:(hash(wid(w))%80)/300-.12,z:Math.sin(a)*r})});W.forEach(w=>{const id=wid(w),b=document.createElement('button');b.className='world';b.dataset.id=id;b.innerHTML=`${String(w.label||id)}<small>${children.get(id).length} Signals</small>`;b.onclick=()=>select(id);list.appendChild(b)});const vs=`attribute vec3 p;attribute float s;attribute vec3 c;uniform mat4 m;varying vec3 v;void main(){gl_Position=m*vec4(p,1.);gl_PointSize=s;v=c;}`,fs=`precision mediump float;varying vec3 v;void main(){float d=distance(gl_PointCoord,vec2(.5));if(d>.5)discard;float g=1.-smoothstep(.08,.5,d);gl_FragColor=vec4(v,g);}`,lvs=`attribute vec3 p;uniform mat4 m;void main(){gl_Position=m*vec4(p,1.);}`,lfs=`precision mediump float;void main(){gl_FragColor=vec4(.48,.58,.78,.25);}`;function shader(t,s){const x=gl.createShader(t);gl.shaderSource(x,s);gl.compileShader(x);if(!gl.getShaderParameter(x,gl.COMPILE_STATUS))throw Error(gl.getShaderInfoLog(x));return x}function program(a,b){const p=gl.createProgram();gl.attachShader(p,shader(gl.VERTEX_SHADER,a));gl.attachShader(p,shader(gl.FRAGMENT_SHADER,b));gl.linkProgram(p);if(!gl.getProgramParameter(p,gl.LINK_STATUS))throw Error(gl.getProgramInfoLog(p));return p}const np=program(vs,fs),lp=program(lvs,lfs),na=gl.getAttribLocation(np,'p'),ns=gl.getAttribLocation(np,'s'),nc=gl.getAttribLocation(np,'c'),nm=gl.getUniformLocation(np,'m'),la=gl.getAttribLocation(lp,'p'),lm=gl.getUniformLocation(lp,'m');let yaw=.15,pitch=.3,distance=4.7,target=distance,selected=null,drag=null,down=null,raf=0;const proj=(f,a,n,z)=>{const q=1/Math.tan(f/2),k=1/(n-z);return new Float32Array([q/a,0,0,0,0,q,0,0,0,0,(z+n)*k,-1,0,0,2*z*n*k,0])},mul=(a,b)=>{const o=new Float32Array(16);for(let c=0;c<4;c++)for(let r=0;r<4;r++)o[c*4+r]=a[r]*b[c*4]+a[4+r]*b[c*4+1]+a[8+r]*b[c*4+2]+a[12+r]*b[c*4+3];return o},view=()=>{const cp=Math.cos(pitch),sp=Math.sin(pitch),cy=Math.cos(yaw),sy=Math.sin(yaw),x=distance*cp*sy,y=distance*sp,z=distance*cp*cy;return new Float32Array([cy,sp*sy,-cp*sy,0,0,cp,sp,0,sy,-sp*cy,cp*cy,0,-x,-y,-z,1])};function nodePosition(n,t){const id=wid(n),c=centers.get(id)||{x:0,y:0,z:0};if(String(n.kind||'').toLowerCase()==='world')return c;const arr=children.get(id)||[],i=Math.max(0,arr.indexOf(n)),q=i/Math.max(1,arr.length)*Math.PI*2+t*.00008*(i%2?-1:1),r=.23+.018*Math.sqrt(i%37);return{x:c.x+Math.cos(q)*r,y:c.y+Math.sin(q)*r*.68,z:c.z+Math.sin(q*2+hash(n.id)%7)*.12}}function select(id){selected=id;target=id?2.35:distance;list.querySelectorAll('button').forEach(b=>b.classList.toggle('active',b.dataset.id===id));if(id){stats.textContent=`${worldNodes.get(id)?.label||id} · ${children.get(id).length} Signals · ${L.length} links`}}function frame(t){raf=requestAnimationFrame(frame);const dpr=Math.min(2,devicePixelRatio||1),w=canvas.clientWidth*dpr,h=canvas.clientHeight*dpr;if(canvas.width!==w||canvas.height!==h){canvas.width=w;canvas.height=h;gl.viewport(0,0,w,h)}distance+=(target-distance)*.08;const m=mul(proj(Math.PI/3,w/h,.04,40),view()),pos=new Map(N.map(n=>[String(n.id),nodePosition(n,t)])),lines=[];for(const e of L){const a=pos.get(String(e.source??e.from)),b=pos.get(String(e.target??e.to));if(a&&b)lines.push(a.x,a.y,a.z,b.x,b.y,b.z)}gl.clearColor(.001,.002,.005,1);gl.clear(gl.COLOR_BUFFER_BIT|gl.DEPTH_BUFFER_BIT);const lb=gl.createBuffer();gl.bindBuffer(gl.ARRAY_BUFFER,lb);gl.bufferData(gl.ARRAY_BUFFER,new Float32Array(lines),gl.DYNAMIC_DRAW);gl.useProgram(lp);gl.uniformMatrix4fv(lm,false,m);gl.enableVertexAttribArray(la);gl.vertexAttribPointer(la,3,gl.FLOAT,false,0,0);gl.drawArrays(gl.LINES,0,lines.length/3);gl.deleteBuffer(lb);const pts=[],stride=7;N.forEach(n=>{const p=nodePosition(n,t),isWorld=String(n.kind||'').toLowerCase()==='world',active=selected&&(wid(n)===selected||String(n.id)===selected);pts.push(p.x,p.y,p.z,isWorld?30:active?6:3.1,isWorld?.16:.35,isWorld?.62:.95,isWorld?1:.86)});const b=gl.createBuffer();gl.bindBuffer(gl.ARRAY_BUFFER,b);gl.bufferData(gl.ARRAY_BUFFER,new Float32Array(pts),gl.DYNAMIC_DRAW);gl.useProgram(np);gl.uniformMatrix4fv(nm,false,m);gl.enableVertexAttribArray(na);gl.vertexAttribPointer(na,3,gl.FLOAT,false,stride*4,0);gl.enableVertexAttribArray(ns);gl.vertexAttribPointer(ns,1,gl.FLOAT,false,stride*4,12);gl.enableVertexAttribArray(nc);gl.vertexAttribPointer(nc,3,gl.FLOAT,false,stride*4,16);gl.drawArrays(gl.POINTS,0,pts.length/stride);gl.deleteBuffer(b)}function project(p,m){const x=m[0]*p.x+m[4]*p.y+m[8]*p.z+m[12],y=m[1]*p.x+m[5]*p.y+m[9]*p.z+m[13],z=m[3]*p.x+m[7]*p.y+m[11]*p.z+m[15];return{x:(x/z*.5+.5)*canvas.clientWidth,y:(-y/z*.5+.5)*canvas.clientHeight}}function hit(e){const r=canvas.getBoundingClientRect(),x=e.clientX-r.left,y=e.clientY-r.top,m=mul(proj(Math.PI/3,canvas.clientWidth/canvas.clientHeight,.04,40),view());let best=null,d=20*20;N.forEach(n=>{const p=project(nodePosition(n,performance.now()),m),dx=p.x-x,dy=p.y-y;if(dx*dx+dy*dy<d){best=wid(n);d=dx*dx+dy*dy}});if(best)select(best)}canvas.addEventListener('click',e=>{if(!down||Math.hypot(e.clientX-down.x,e.clientY-down.y)<5)hit(e)});canvas.addEventListener('pointerdown',e=>{down={x:e.clientX,y:e.clientY};drag={x:e.clientX,y:e.clientY};canvas.classList.add('drag');canvas.setPointerCapture(e.pointerId)});canvas.addEventListener('pointermove',e=>{if(!drag)return;yaw+=(e.clientX-drag.x)*.006;pitch=Math.max(-1.2,Math.min(1.2,pitch+(e.clientY-drag.y)*.004));drag={x:e.clientX,y:e.clientY}});canvas.addEventListener('pointerup',()=>{drag=null;canvas.classList.remove('drag')});canvas.addEventListener('wheel',e=>{e.preventDefault();target=Math.max(1.5,Math.min(9,target*(e.deltaY<0?.88:1.14)))},{passive:false});canvas.addEventListener('webglcontextlost',e=>{e.preventDefault();cancelAnimationFrame(raf)});canvas.addEventListener('webglcontextrestored',()=>location.reload());frame(0)})();</script></body></html>'''
    return template.replace("__TITLE__", safe).replace("__DATA__", data)


def _render_solar_atlas(graph: Mapping[str, Any], title: str) -> str:
    """Render the global Atlas with dense linked constellations and colored Signals."""
    data = _script_json(_validated_graph(graph))
    safe = title.replace("&", "&amp;").replace("<", "&lt;").replace(">", "&gt;")
    template = r'''<!doctype html><html lang="en"><head><meta charset="utf-8"><meta name="viewport" content="width=device-width,initial-scale=1"><meta name="holocore-atlas-viewer" content="6"><title>__TITLE__</title><style>:root{color-scheme:dark;font:13px "IBM Plex Sans","Segoe UI",system-ui,sans-serif}*{box-sizing:border-box}body{margin:0;background:#000;color:#dbe7f7;overflow:hidden}header{height:58px;padding:10px 16px;display:flex;align-items:center;gap:14px;background:#090b11;border-bottom:1px solid #252c3b;position:relative;z-index:3}h1{margin:0;color:#b8cced;font-size:15px}#stats{margin-left:auto;color:#8c9ab0}#hint{color:#6e7b91;font-size:12px}#stage{position:relative;height:calc(100vh - 58px);background:radial-gradient(circle at 56% 48%,#172033 0,#070a11 47%,#000 88%)}canvas{position:absolute;inset:0;width:100%;height:100%;display:block}#gl{cursor:grab}#gl.drag{cursor:grabbing}#edges{pointer-events:none}#panel{position:absolute;z-index:2;left:16px;top:16px;width:234px;max-height:calc(100% - 32px);overflow:auto;padding:10px;border:1px solid #293448;border-radius:8px;background:#0a0d15e8;box-shadow:0 12px 32px #000a;backdrop-filter:blur(10px)}.world{display:block;width:100%;margin:5px 0;padding:8px 9px;border:1px solid #2b3950;border-radius:6px;background:#111722;color:#d9e7fb;text-align:left;cursor:pointer}.world:hover,.world.active{border-color:#73a5ff;background:#18243a}.world small{display:block;color:#7f8da3;margin-top:2px}.legend{margin-top:10px;color:#8997ac;font-size:11px;line-height:1.7}</style></head><body><header><h1>__TITLE__</h1><span id="stats"></span><span id="hint">click a World to focus · drag to orbit · wheel to zoom</span></header><div id="stage"><aside id="panel"></aside><canvas id="edges"></canvas><canvas id="gl"></canvas></div><script id="data" type="application/json">__DATA__</script><script>
(()=>{'use strict';const D=JSON.parse(document.getElementById('data').textContent),N=D.nodes||[],L=D.links||D.edges||[],W=N.filter(n=>String(n.kind||'').toLowerCase()==='world'),stage=document.getElementById('stage'),points=document.getElementById('gl'),edges=document.getElementById('edges'),panel=document.getElementById('panel'),stats=document.getElementById('stats');const hash=s=>{let h=2166136261;for(const c of String(s)){h^=c.charCodeAt(0);h=Math.imul(h,16777619)}return h>>>0},wid=n=>String(n.world||n.id.split(':')[0]),worlds=new Map(W.map(w=>[wid(w),w])),children=new Map(W.map(w=>[wid(w),[]]));N.forEach(n=>{if(String(n.kind||'').toLowerCase()!=='world'&&children.has(wid(n)))children.get(wid(n)).push(n)});stats.textContent=`${W.length} Worlds · ${N.length} Signals · ${L.length} links`;panel.innerHTML=W.map(w=>`<button class="world" data-world="${wid(w)}">${String(w.label||wid(w))}<small>${children.get(wid(w)).length} Signals</small></button>`).join('')+`<div class="legend">● Worlds &nbsp; ● Concepts &nbsp; ● Files<br>━ Atlas relationships</div>`;const gl=points.getContext('webgl2',{antialias:true,alpha:true})||points.getContext('webgl',{antialias:true,alpha:true});if(!gl)return;const vs=`attribute vec3 p;attribute float s;attribute vec3 c;uniform mat4 m;varying vec3 v;void main(){gl_Position=m*vec4(p,1.);gl_PointSize=s;v=c;}`,fs=`precision mediump float;varying vec3 v;void main(){float d=distance(gl_PointCoord,vec2(.5));if(d>.5)discard;gl_FragColor=vec4(v,1.-smoothstep(.05,.5,d));}`;function compile(t,s){const x=gl.createShader(t);gl.shaderSource(x,s);gl.compileShader(x);if(!gl.getShaderParameter(x,gl.COMPILE_STATUS))throw Error(gl.getShaderInfoLog(x));return x}const p=gl.createProgram();gl.attachShader(p,compile(gl.VERTEX_SHADER,vs));gl.attachShader(p,compile(gl.FRAGMENT_SHADER,fs));gl.linkProgram(p);if(!gl.getProgramParameter(p,gl.LINK_STATUS))throw Error(gl.getProgramInfoLog(p));const ap=gl.getAttribLocation(p,'p'),as=gl.getAttribLocation(p,'s'),ac=gl.getAttribLocation(p,'c'),um=gl.getUniformLocation(p,'m');let yaw=.1,pitch=.25,distance=4.2,target=distance,selected=null,drag=null,down=null,raf=0;const palette=['#6ee7f9','#8b9cff','#d58cff','#ff9d7a','#ffd166','#62e6a7','#e98cff'];const color=n=>{if(String(n.kind||'').toLowerCase()==='world')return '#ffffff';return palette[hash(n.kind||n.type||n.id)%palette.length]};const proj=(f,a,n,z)=>{const q=1/Math.tan(f/2),k=1/(n-z);return new Float32Array([q/a,0,0,0,0,q,0,0,0,0,(z+n)*k,-1,0,0,2*z*n*k,0])},mul=(a,b)=>{const o=new Float32Array(16);for(let c=0;c<4;c++)for(let r=0;r<4;r++)o[c*4+r]=a[r]*b[c*4]+a[4+r]*b[c*4+1]+a[8+r]*b[c*4+2]+a[12+r]*b[c*4+3];return o},view=()=>{const cp=Math.cos(pitch),sp=Math.sin(pitch),cy=Math.cos(yaw),sy=Math.sin(yaw),x=distance*cp*sy,y=distance*sp,z=distance*cp*cy;return new Float32Array([cy,sp*sy,-cp*sy,0,0,cp,sp,0,sy,-sp*cy,cp*cy,0,-x,-y,-z,1])},centers=new Map();W.forEach((w,i)=>{const a=i/Math.max(1,W.length)*Math.PI*2,r=1.15+Math.sqrt(i+1)*.3;centers.set(wid(w),{x:Math.cos(a)*r,y:(hash(wid(w))%100)/260-.18,z:Math.sin(a)*r})});function pos(n,t){const c=centers.get(wid(n))||{x:0,y:0,z:0};if(String(n.kind||'').toLowerCase()==='world')return c;const a=children.get(wid(n))||[],i=Math.max(0,a.indexOf(n)),h=hash(n.id),q=(h%6283)/1000+t*.000015*(i%2?-1:1),r=.16+(h%1000)/1000*.78;return{x:c.x+Math.cos(q)*r,y:c.y+Math.sin(q)*r*.72,z:c.z+Math.sin(q*1.7)*.35}}function project(v,m,w,h){const x=m[0]*v.x+m[4]*v.y+m[8]*v.z+m[12],y=m[1]*v.x+m[5]*v.y+m[9]*v.z+m[13],q=m[3]*v.x+m[7]*v.y+m[11]*v.z+m[15];return{x:(x/q*.5+.5)*w,y:(-y/q*.5+.5)*h,z:q}}function select(id){selected=id;target=id?2.2:distance;panel.querySelectorAll('.world').forEach(b=>b.classList.toggle('active',b.dataset.world===id));if(id)stats.textContent=`${worlds.get(id).label||id} · ${children.get(id).length} Signals · ${L.length} links`}panel.querySelectorAll('.world').forEach(b=>b.onclick=()=>select(b.dataset.world));function frame(t){raf=requestAnimationFrame(frame);const dpr=Math.min(2,devicePixelRatio||1),w=stage.clientWidth*dpr,h=stage.clientHeight*dpr;for(const c of [points,edges]){if(c.width!==w||c.height!==h){c.width=w;c.height=h}}gl.viewport(0,0,w,h);distance+=(target-distance)*.06;const m=mul(proj(Math.PI/3,w/h,.04,40),view()),positions=new Map(N.map(n=>[String(n.id),pos(n,t)]));const ctx=edges.getContext('2d');ctx.setTransform(dpr,0,0,dpr,0,0);ctx.clearRect(0,0,stage.clientWidth,stage.clientHeight);ctx.lineWidth=.55;for(const e of L){const a=positions.get(String(e.source??e.from)),b=positions.get(String(e.target??e.to));if(!a||!b)continue;const A=project(a,m,stage.clientWidth,stage.clientHeight),B=project(b,m,stage.clientWidth,stage.clientHeight);if(A.z<0||B.z<0)continue;ctx.strokeStyle=selected&&(wid(N.find(n=>String(n.id)===(e.source??e.from))||{})===selected||wid(N.find(n=>String(n.id)===(e.target??e.to))||{})===selected)?'rgba(125,211,252,.72)':'rgba(160,180,220,.28)';ctx.beginPath();ctx.moveTo(A.x,A.y);ctx.lineTo(B.x,B.y);ctx.stroke()}const pts=[],stride=7;for(const n of N){const v=pos(n,t),isWorld=String(n.kind||'').toLowerCase()==='world',active=selected&&(wid(n)===selected||String(n.id)===selected),rgb=color(n).match(/\w\w/g).map(x=>parseInt(x,16)/255);pts.push(v.x,v.y,v.z,isWorld?31:active?7:3.5,rgb[0],rgb[1],rgb[2])}gl.clearColor(0,0,0,0);gl.clear(gl.COLOR_BUFFER_BIT|gl.DEPTH_BUFFER_BIT);gl.useProgram(p);gl.uniformMatrix4fv(um,false,m);const b=gl.createBuffer();gl.bindBuffer(gl.ARRAY_BUFFER,b);gl.bufferData(gl.ARRAY_BUFFER,new Float32Array(pts),gl.STREAM_DRAW);gl.enableVertexAttribArray(ap);gl.vertexAttribPointer(ap,3,gl.FLOAT,false,stride*4,0);gl.enableVertexAttribArray(as);gl.vertexAttribPointer(as,1,gl.FLOAT,false,stride*4,12);gl.enableVertexAttribArray(ac);gl.vertexAttribPointer(ac,3,gl.FLOAT,false,stride*4,16);gl.drawArrays(gl.POINTS,0,pts.length/stride);gl.deleteBuffer(b)}function hit(e){const r=points.getBoundingClientRect(),m=mul(proj(Math.PI/3,stage.clientWidth/stage.clientHeight,.04,40),view()),x=e.clientX-r.left,y=e.clientY-r.top;let best=null,d=24*24;N.forEach(n=>{const q=project(pos(n,performance.now()),m,stage.clientWidth,stage.clientHeight),dx=q.x-x,dy=q.y-y;if(dx*dx+dy*dy<d){best=wid(n);d=dx*dx+dy*dy}});if(best)select(best)}points.addEventListener('click',e=>{if(!down||Math.hypot(e.clientX-down.x,e.clientY-down.y)<5)hit(e)});points.addEventListener('pointerdown',e=>{down={x:e.clientX,y:e.clientY};drag={x:e.clientX,y:e.clientY};points.classList.add('drag');points.setPointerCapture(e.pointerId)});points.addEventListener('pointermove',e=>{if(!drag)return;yaw+=(e.clientX-drag.x)*.006;pitch=Math.max(-1.2,Math.min(1.2,pitch+(e.clientY-drag.y)*.004));drag={x:e.clientX,y:e.clientY}});points.addEventListener('pointerup',()=>{drag=null;points.classList.remove('drag')});points.addEventListener('wheel',e=>{e.preventDefault();target=Math.max(1.3,Math.min(9,target*(e.deltaY<0?.88:1.14)))},{passive:false});points.addEventListener('webglcontextlost',e=>{e.preventDefault();cancelAnimationFrame(raf)});points.addEventListener('webglcontextrestored',()=>location.reload());frame(0)})();</script></body></html>'''
    return template.replace("__TITLE__", safe).replace("__DATA__", data)


def generate_atlas_html(
    atlas: str | Path | Mapping[str, Any], output: str | Path | None = None, *, title: str = "HoloCore Atlas"
) -> Path:
    if isinstance(atlas, Mapping):
        if output is None:
            raise ValueError("output is required when atlas is an in-memory mapping")
        destination = Path(output)
    else:
        source = Path(atlas)
        destination = Path(output) if output is not None else source.with_suffix(".html")
    return generate_atlas_views(atlas, [destination], title=title)[0]


def generate_atlas_views(
    atlas: str | Path | Mapping[str, Any],
    outputs: Iterable[str | Path],
    *,
    title: str = "HoloCore Atlas",
) -> list[Path]:
    """Render once and write the same viewer to all public/compatibility paths."""
    destinations = list(dict.fromkeys(Path(output) for output in outputs))
    if not destinations:
        raise ValueError("at least one Atlas HTML output is required")
    if isinstance(atlas, Mapping):
        graph = atlas
    else:
        source = Path(atlas)
        try:
            graph = json.loads(source.read_text(encoding="utf-8"))
        except (OSError, json.JSONDecodeError) as exc:
            raise ValueError(f"Cannot read Atlas graph {source}: {exc}") from exc
    rendered = render_atlas_html(graph, title=title)
    for destination in destinations:
        destination.parent.mkdir(parents=True, exist_ok=True)
        destination.write_text(rendered, encoding="utf-8", newline="\n")
    return destinations


write_atlas_html = generate_atlas_html
build_atlas_html = generate_atlas_html

__all__ = [
    "ATLAS_VIEWER_VERSION", "render_atlas_html", "generate_atlas_html",
    "generate_atlas_views", "write_atlas_html", "build_atlas_html",
]
