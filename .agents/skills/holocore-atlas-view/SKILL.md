---
name: holocore-atlas-view
description: Generate and open the self-contained Atlas HTML view.
---

# holocore-atlas-view

Generate and open the self-contained Atlas HTML view.

This is read-only.

Interpret any details following the skill mention as the user's arguments. Run the corresponding command from the project root, adding those arguments where appropriate:

```text
holocore atlas-view
```
