---
name: holocore-sync-all
description: Reconcile and refresh every registered World.
---

# holocore-sync-all

Reconcile and refresh every registered World.

This may write project data; confirm the requested scope before running it.

Interpret any details following the skill mention as the user's arguments. Run the corresponding command from the project root, adding those arguments where appropriate:

```text
holocore sync-all
```
